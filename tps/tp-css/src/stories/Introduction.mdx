import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />

# Introduction au TP de CSS

Bienvenue dans ce parcours complet consacré aux bases essentielles du CSS.
L’objectif de ce TP est de comprendre **comment les navigateurs interprètent les styles**, **comment les éléments se comportent dans la page**, et **comment maîtriser précisément leur apparence**.

Ce module est composé de 6 grandes parties. Elles ont été organisées dans un ordre logique :
du fonctionnement interne du CSS → à la structure des éléments → puis leur affichage → jusqu’à leur positionnement précis.

---

## 📘 Sommaire du TP

### 1. [Les sélecteurs](?path=/docs/css_selecteurs-lesson--introduction)
Avant tout, il faut savoir **cibler les éléments** sur lesquels appliquer nos styles.
Dans cette partie, vous apprendrez les différents types de sélecteurs CSS (élément, classe, ID, attributs, pseudo-classes, pseudo-éléments…) et comment les combiner efficacement.

---

### 2. [La cascade, la spécificité et l’héritage](?path=/docs/css_css-casquade-lesson--introduction)
Une fois qu’on sait *où appliquer* un style, il faut comprendre **comment le navigateur décide quel style gagne**.
Cette section explique :
- la cascade,
- la spécificité,
- l’héritage,
- et le fonctionnement du `!important`.

C’est l’un des fondements les plus importants du langage.

---

### 3. [Le Box Model](?path=/docs/css_box-model-lesson--introduction)
Chaque élément HTML est une **boîte**, constituée de :
- `content`
- `padding`
- `border`
- `margin`

Vous apprendrez comment ces couches s’articulent, comment les tailles réagissent selon les propriétés, et comment le `box-sizing` influence tout le reste.

---

### 4. [Les unités CSS](?path=/docs/css_unites-lesson--introduction)
Pour manipuler tailles, espacements, ou positions, il faut choisir la bonne unité :
- unités absolues (`px`)
- unités relatives (`em`, `rem`)
- unités liées au viewport (`vw`, `vh`)
- unités modernes (`min()`, `max()`, `clamp()`)

Vous apprendrez **comment choisir la meilleure unité selon le contexte**, notamment pour créer des mises en page fluides.

---

### 5. [Le display et les modes d’affichage](?path=/docs/css_display-lesson--introduction)
Cette partie explique comment un élément s’affiche dans le flux :
- `block`
- `inline`
- `inline-block`
- `flex`
- `grid`

Vous apprendrez comment ces valeurs influencent :
- l’occupation de la ligne,
- la gestion des dimensions,
- l’agencement des enfants.

C’est une étape clé avant de passer au positionnement.

---

### 6. [Le positionnement CSS](?path=/docs/css_positionnement-lesson--introduction)
Enfin, vous verrez comment **placer les éléments avec précision**, grâce aux propriétés :
- `position: static`, `relative`, `absolute`, `fixed`, `sticky`
- `top`, `right`, `bottom`, `left`
- `z-index`

Cette section combine toutes les notions précédentes pour comprendre comment les éléments se superposent, se déplacent et s’ancrent dans la page.

---

# 🎯 Objectifs du TP

À la fin de ce parcours, vous serez capable de :

- comprendre comment fonctionne le CSS de bas niveau (cascade, héritage, spécificité)
- manipuler efficacement le box model
- choisir la bonne unité dans un contexte donné
- contrôler l’affichage des éléments avec `display`
- positionner précisément des éléments dans une page
- créer des interfaces cohérentes, lisibles et flexibles

---

# 🚀 Bonne découverte !

Parcourez les chapitres dans l’ordre pour progresser naturellement, ou utilisez le sommaire pour accéder directement à la partie souhaitée.
